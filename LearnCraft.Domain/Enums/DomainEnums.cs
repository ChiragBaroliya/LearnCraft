namespace LearnCraft.Domain.Enums;

public enum UserRole
{
    Student = 0,
    Instructor = 1,
    Admin = 2
}

public enum ContentType
{
    Video = 0,
    Document = 1
}

public enum CourseStatus
{
    Draft = 0,
    Published = 1,
    Archived = 2
}

